<UserControl x:Class="WpfHanoiTowers.HanoiTowers"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:HelixToolkit="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
             xmlns:local="clr-namespace:WpfHanoiTowers"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <!-- Place a Label control at the top of the view. -->
        <Label 
                HorizontalAlignment="Center" 
                TextBlock.TextAlignment="Center" 
                FontSize="20" 
                Foreground="Red" 
                Content="Towers of Hanoi"/>

        <!-- Viewport3D is the rendering surface. -->
        <HelixToolkit:HelixViewport3D ZoomExtentsWhenLoaded="True" Name="myViewport" Margin="0,-10,10,10"
                                      Mouse.MouseDown="MyViewport_MouseDown" Mouse.MouseUp="MyViewport_MouseUp" Mouse.MouseMove="MyViewport_MouseMove">
            <HelixToolkit:SunLight/>
            <HelixToolkit:GridLinesVisual3D Width="8" Length="18" MinorDistance="1" MajorDistance="1" Thickness="0.01"/>
            <!-- columns and disks are added here by code -->
        </HelixToolkit:HelixViewport3D>
    </Grid>
</UserControl>
